from abc import ABC
from typing import Optional

from .coords import Coords


class Space(ABC):
    # def __init__(
    #     self, local: Optional[Coords] = None, global_: Optional[Coords] = None
    # ):
        pass
